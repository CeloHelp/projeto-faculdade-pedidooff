# Configurações da aplicação PedidoOff
# Caso você opte por JPA/Hibernate, descomente e ajuste as propriedades.

# Caminho do banco SQLite: ${user.home}/PedidoFacil/pedido_facil.db
# Para garantir que a pasta existe, crie-a antes de rodar a aplicação.

# spring.datasource.url=jdbc:sqlite:${user.home}/PedidoFacil/pedido_facil.db
# spring.datasource.driver-class-name=org.sqlite.JDBC
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.database-platform=org.hibernate.dialect.SQLiteDialect
